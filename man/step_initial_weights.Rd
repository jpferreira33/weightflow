% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nonprob_entry.R
\name{step_initial_weights}
\alias{step_initial_weights}
\title{Set the initial weights of a nonprobability sample}
\usage{
step_initial_weights(
  spec,
  method = c("unity", "naive", "constant", "ipa"),
  pop_size = NULL,
  value = NULL,
  reference = NULL,
  reference_weights = NULL,
  formula = NULL
)
}
\arguments{
\item{spec}{a weighting_spec (typically from \code{\link[=weighting_spec_nonprob]{weighting_spec_nonprob()}}).}

\item{method}{one of:
\itemize{
\item \code{"unity"} (default): unit weight for every case, normalized to sum to
\code{pop_size} if given (the unity-with-normalization method, which performs
well in simulations).
\item \code{"naive"}: weight 1 for every case, no normalization (a baseline that
treats the sample as if it were already representative).
\item \code{"constant"}: a constant weight \code{value} for every case (e.g. N/n).
\item \code{"ipa"}: inclusion-probability-adjusted; each unit gets the design weight
of the most similar reference unit (nearest on the covariates). Requires
\code{reference}, \code{reference_weights} and \code{formula}.
}}

\item{pop_size}{optional population size for normalization (\code{"unity"}).}

\item{value}{the constant weight for \code{method = "constant"}.}

\item{reference, reference_weights, formula}{needed for \code{method = "ipa"}.}
}
\value{
the spec with the step appended.
}
\description{
First step of a nonprobability recipe: it fixes the starting weights of the
sample before the propensity adjustment. Several conventions from the
literature are offered.
}
\seealso{
\code{\link[=weighting_spec_nonprob]{weighting_spec_nonprob()}}, \code{\link[=step_pseudo_weight]{step_pseudo_weight()}}
}
