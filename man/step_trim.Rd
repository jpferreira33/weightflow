% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spec-steps-calibrate.R
\name{step_trim}
\alias{step_trim}
\title{Trim extreme weights against a ratio}
\usage{
step_trim(
  spec,
  max_ratio,
  min_ratio = NULL,
  reference = c("base", "median", "value"),
  redistribute = TRUE,
  by = NULL,
  maxit = 50L
)
}
\arguments{
\item{spec}{a weighting_spec.}

\item{max_ratio}{number. Upper cap. Its meaning depends on \code{reference}. E.g.
with reference = "base" and max_ratio = 4, no weight may exceed 4 times its
design weight.}

\item{min_ratio}{number or NULL. Lower floor (same units as max_ratio).}

\item{reference}{"base" (multiple of each unit's base weight),
"median" (multiple of the median of current weights) or
"value" (absolute weight value).}

\item{redistribute}{logical. If TRUE, redistributes the trimmed excess among
the uncapped weights to preserve the total (iterating). If you calibrate
afterwards you can use FALSE: calibration restores the totals.}

\item{by}{character. Groups within which to redistribute (optional).}

\item{maxit}{integer. Maximum cap+redistribution iterations.}
}
\value{
The input \code{weighting_spec} with this step appended to its recipe. The
step is recorded only; it is evaluated when \code{prep()} is called.
}
\description{
Caps the current weights at a multiple of a reference (each unit's base
weight, the group median, or an absolute value) and, by default, redistributes
the removed mass among the untrimmed units so the weighted total survives the
trim. With \code{by}, the reference and the cap are computed separately within each
subgroup. An optional step that can appear anywhere in the recipe, more than
once.
}
\details{
There is no standard threshold: \code{max_ratio} is an analyst decision, a
bias-variance trade-off. Use Kish's design effect (see summary) to judge
whether trimming is worth it.
}
\examples{
weighting_spec(sample_survey, base_weights = pw) |>
  step_trim(max_ratio = 3, reference = "base")
}
