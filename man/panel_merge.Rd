% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/panel-design.R
\name{panel_merge}
\alias{panel_merge}
\title{Build the wide longitudinal file from per-wave surveys}
\usage{
panel_merge(
  waves,
  by,
  responded = NULL,
  require = c("any", "all"),
  suffix = "_"
)
}
\arguments{
\item{waves}{a \strong{named} list of per-wave \code{data.frame}s; the names become the
wave labels and column suffixes.}

\item{by}{one or more column names (a character vector) forming the unit key,
present in every wave -- e.g. \code{"ID"} for a household or \code{c("ID", "nper")} for
a person.}

\item{responded}{optional string: the name of a response indicator present in
every wave, used to build \verb{.wf_resp_<wave>}.}

\item{require}{one of \code{"any"} (union of units, default) or \code{"all"}
(intersection).}

\item{suffix}{string inserted before the wave label when renaming columns
(default \code{"_"}), e.g. \code{edad} in wave \code{T1} becomes \code{edad_T1}.}
}
\value{
a wide \code{data.frame}, one row per unit, ready for \code{\link[=panel_design]{panel_design()}} /
\code{weighting_spec()}.
}
\description{
Joins a named list of per-wave \code{data.frame}s on a stable unit key into the
wide, one-row-per-unit file that the longitudinal recipe consumes. Each wave's
non-key columns are suffixed with the wave name, and per-wave presence
indicators \verb{.wf_in_<wave>} (and, if \code{responded} is given, response indicators
\verb{.wf_resp_<wave>}) are added so a later \code{step_attrition()} can model the
response pattern.
}
\details{
\code{require = "any"} (the default) keeps every unit seen in at least one wave --
which the attrition model needs, because it has to be fitted over responders
\emph{and} non-responders. \code{require = "all"} keeps only the intersection (CEPAL's
\verb{s(2) = s1 cap s2 cap ...}); prefer to reach it with \code{step_attrition()} after
estimating, not by dropping units before the model can see them.
}
\examples{
# reshape two waves of the long panel into one wide, one-row-per-unit file
wide <- panel_merge(
  list(T1 = subset(panel_ine, ola == 1), T2 = subset(panel_ine, ola == 2)),
  by = c("id_hogar", "nper"), require = "any")
names(wide)                       # per-wave columns are suffixed _T1 / _T2
}
\seealso{
\code{\link[=panel_design]{panel_design()}}
}
