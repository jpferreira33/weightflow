% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/panel-design.R
\name{panel_pr}
\alias{panel_pr}
\title{Panel-selection probability for a set of combined waves}
\usage{
panel_pr(object, waves = NULL)
}
\arguments{
\item{object}{a \code{wf_panel_design} (from \code{\link[=panel_design]{panel_design()}}).}

\item{waves}{optional character vector of the combined waves; defaults to all.}
}
\value{
a single probability in \verb{(0, 1]}.
}
\description{
Returns \verb{Pr(panel selection)} for combining \code{waves} in a \code{\link[=panel_design]{panel_design()}} that
carries a \code{rotation_group}: the fraction of rotation-group cohorts present in
\emph{all} the combined waves. Its reciprocal is the CEPAL panel base-weight factor
used by \code{\link[=step_panel_overlap]{step_panel_overlap()}} (\code{prob = panel_pr(pd, c("T1","T2"))}). Surveys
without a public rotation-group variable (e.g. the Chilean ENE) cannot use this
-- derive the probability another way and pass it to \code{step_panel_overlap()}.
}
\examples{
pd <- panel_design(panel_ine, unit = c("id_hogar", "nper"), wave = "ola",
                   rotation_group = "grupo_rotacion")
panel_pr(pd)                    # over all waves
panel_pr(pd, c("1", "2"))       # combining waves 1 and 2
}
\seealso{
\code{\link[=panel_design]{panel_design()}}, \code{\link[=step_panel_overlap]{step_panel_overlap()}}
}
