% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/recipe-io.R
\name{read_recipe}
\alias{read_recipe}
\title{Read a weighting recipe from a YAML file}
\usage{
read_recipe(file, data = NULL, references = NULL)
}
\arguments{
\item{file}{path to the recipe \code{.yml}/\code{.yaml} file.}

\item{data}{optional data frame. When supplied, the recipe is rebuilt into a
\code{weighting_spec} on this data. The columns the steps reference (weights,
auxiliaries, disposition flags) must exist in \code{data}.}

\item{references}{optional named list of \code{reference_sample()} objects, named by
the id of the step that uses each one, to restore the steps that calibrate or
pseudo-weight against a reference (whose microdata the recipe does not store).}
}
\value{
With \code{data = NULL}, a \code{weightflow_recipe} manifest (a list with a print
method). With \code{data}, a \code{weighting_spec}.
}
\description{
Reads a recipe written by \code{\link[=write_recipe]{write_recipe()}}. With \code{data = NULL} (the default) it
returns an inspectable recipe manifest (for review or archival); pass \code{data} to
reconstruct an executable \code{weighting_spec} bound to that data, ready for \code{\link[=prep]{prep()}}.
}
\details{
A reconstructed recipe is validated only when you call \code{\link[=prep]{prep()}}: a hand-edited
recipe with an out-of-range or mistyped value surfaces its error there, not at
\code{read_recipe()} time. And because reading a recipe evaluates the stored
expressions, only read recipes you trust, as you would \code{source()} an R script.
}
\examples{
spec <- weighting_spec(sample_survey, base_weights = pw) |>
  step_nonresponse(respondent = responded, method = "weighting_class", by = "region")
f <- tempfile(fileext = ".yml"); write_recipe(spec, f)
read_recipe(f)                       # inspect the manifest
spec2 <- read_recipe(f, data = sample_survey)   # rebuild an executable recipe
}
\seealso{
\code{\link[=write_recipe]{write_recipe()}}

Other recipe serialization:
\code{\link[=write_recipe]{write_recipe()}}
}
\concept{recipe serialization}
