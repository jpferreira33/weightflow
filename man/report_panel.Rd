% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report-panel.R
\name{report_panel}
\alias{report_panel}
\title{Panel / longitudinal HTML report}
\usage{
report_panel(
  design = NULL,
  change = NULL,
  longitudinal = NULL,
  transition = NULL,
  coordinated = NULL,
  estimates = NULL,
  variance = NULL,
  file = NULL,
  open = TRUE,
  lang = c("en", "es")
)
}
\arguments{
\item{design}{a \code{wf_panel_design} from \code{\link[=panel_design]{panel_design()}}.}

\item{change}{a \code{weightflow_change} from \code{\link[=change_estimate]{change_estimate()}} / \code{\link[=change_mean]{change_mean()}}.}

\item{longitudinal}{a prepped longitudinal \code{weighting_spec}; when given, the report is the full
weighting report of that weight with the panel section added.}

\item{transition}{a \code{weightflow_transition} / \verb{_boot} from the flow functions.}

\item{coordinated}{the coordinated \code{\link[=wave_bootstrap]{wave_bootstrap()}} / \code{\link[=wave_jackknife]{wave_jackknife()}} object behind
\code{change}, so the report shows a "Replication-based variance estimation" card stating the
coordinated method, replicates, strata, PSUs, recipe-aware setting and seed.}

\item{estimates}{results of the estimation grammar: a \code{weightflow_estimation_result} from
\code{\link[=collect_estimates]{collect_estimates()}}, an uncollected \code{\link[=step_estimate]{step_estimate()}} pipeline (collected here), or a
named list of either -- the names become the card titles. Each becomes a results block
with the levels behind a change, its coordinated standard error and confidence interval,
a dot-and-whisker chart across the \code{\link[=step_domain]{step_domain()}} cells, and the subpopulation any
\code{\link[=step_filter]{step_filter()}} restricted it to.}

\item{variance}{the longitudinal-weight \code{\link[=bootstrap_weights]{bootstrap_weights()}} / jackknife object, so the base
report shows the variance/replication card (deff, method, replicates) for the longitudinal
weight instead of "replicate weights not created". The coordinated CHANGE variance is shown by
the \code{change} card.}

\item{file}{output path; a temporary \code{.html} if \code{NULL}.}

\item{open}{open the file in a browser.}

\item{lang}{\code{"en"} (default) or \code{"es"}.}
}
\value{
the path to the written HTML file, invisibly.
}
\description{
Builds an HTML report by ADDING the panel cards to the standard report. When a longitudinal
weight is given, it renders the full \code{\link[=report_weighting]{report_weighting()}} page for that weight (cascade, weight
distribution, deff, ...) and injects a "Panel / longitudinal" section with the rotation
structure (\code{\link[=panel_design]{panel_design()}}), the coordinated net-change variance (\code{\link[=change_estimate]{change_estimate()}}), the
attrition/retention summary, and the gross flows (\code{\link[=transition_matrix]{transition_matrix()}} / \code{\link[=boot_transition]{boot_transition()}}).
Without a longitudinal weight it writes a small standalone page with those cards. Any argument
may be \code{NULL}; its card is skipped.
}
\seealso{
\code{\link[=report_weighting]{report_weighting()}}, \code{\link[=panel_design]{panel_design()}}, \code{\link[=change_estimate]{change_estimate()}}, \code{\link[=boot_transition]{boot_transition()}}
}
