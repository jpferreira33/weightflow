% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/variance.R
\name{two_phase_variance}
\alias{two_phase_variance}
\title{Decompose a two-phase variance into V = V1 + V2}
\usage{
two_phase_variance(
  object,
  variable,
  estimator = c("mean", "total"),
  replicates = 500L,
  seed = NULL,
  fpc = NULL
)
}
\arguments{
\item{object}{a \code{weighting_spec} (or prepped) whose recipe contains
\code{\link[=step_subsample]{step_subsample()}}.}

\item{variable}{name of the study variable (a single column).}

\item{estimator}{\code{"mean"} (default) or \code{"total"}.}

\item{replicates, seed, fpc}{passed through to \code{\link[=bootstrap_weights]{bootstrap_weights()}}.}
}
\value{
An object of class \code{weightflow_tp_variance}: \code{V1}, \code{V2}, \code{V}, the
matching standard errors \code{se1}, \code{se2}, \code{se}, and \code{prop_phase2} = V2 / V.
}
\description{
For a recipe containing \code{\link[=step_subsample]{step_subsample()}}, \code{two_phase_variance()} splits the
recipe-aware bootstrap variance of an estimate into its first-phase and
second-phase components, \eqn{V = V_1 + V_2}. The per-unit coupling factor has
variance \eqn{d = (1-f_1)\pi_2 + (1-\pi_2)}, the sum of the phase-1 term
\eqn{(1-f_1)\pi_2} and the phase-2 conditional term \eqn{1-\pi_2}; running the
same bootstrap with each term in turn yields the two components.
}
\details{
The share \code{prop_phase2} = V2 / V is an operational read: a large share means
the second-phase subsampling drives the uncertainty, so subsampling more would
pay off; a small share means a denser (more expensive) subsample would buy
little, and the first phase is the binding constraint.
}
\examples{
\donttest{
df <- transform(sample_survey,
                in2 = as.integer(runif(nrow(sample_survey)) < 0.3), p2 = 0.3)
spec <- weighting_spec(df, base_weights = pw) |>
  step_subsample(selected = in2, prob = p2, psu = "household_id")
two_phase_variance(spec, "income", replicates = 100)
}
}
\seealso{
\code{\link[=bootstrap_weights]{bootstrap_weights()}}, \code{\link[=step_subsample]{step_subsample()}}.
}
