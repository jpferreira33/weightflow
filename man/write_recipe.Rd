% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/recipe-io.R
\name{write_recipe}
\alias{write_recipe}
\title{Write a weighting recipe to a YAML file}
\usage{
write_recipe(object, file, timestamp = TRUE)
}
\arguments{
\item{object}{a \code{weighting_spec} (or a prepped one; only the recipe is written,
never the weights or the data).}

\item{file}{path to the \code{.yml}/\code{.yaml} file to write.}

\item{timestamp}{whether to record the write time (UTC) in the file. Default
\code{TRUE}; set \code{FALSE} for byte-identical output across writes of the same recipe
(cleaner version-control diffs).}
}
\value{
the \code{file} path, invisibly.
}
\description{
Serializes the recipe (the weighting \emph{method}, not the data) to a human-readable
YAML file: the base-weight column, the non-probability flag, and every step's id,
type and parameters. The file is a versionable metadata artifact you can review
in a pull request, archive next to the quality report, or reference from a
metadata system. Read it back with \code{\link[=read_recipe]{read_recipe()}}.
}
\details{
Formulas and captured column expressions are stored as text. A \code{reference_sample()}
is stored as a descriptor only (its microdata is not metadata), so a step that
calibrates or pseudo-weights against a reference must be given that reference
again when the recipe is reconstructed. Small control-totals tables (for example
a tidy poststratification total) are serialized in full; a data frame larger than
10,000 rows is treated as microdata and rejected (route it through
\code{reference_sample()}).

A recipe is portable only if its captured expressions reference columns of the
data (for example \code{respondent = responded}). An expression that referenced
objects from your R session (say \code{respondent = id \%in\% ids_resp}) is stored as
text but cannot be reconstructed elsewhere, and will error at \code{prep()}.
}
\examples{
spec <- weighting_spec(sample_survey, base_weights = pw) |>
  step_nonresponse(respondent = responded, method = "weighting_class", by = "region") |>
  step_calibrate(method = "raking", margins = list(region = c(table(population$region))))
f <- tempfile(fileext = ".yml")
write_recipe(spec, f)
}
\seealso{
\code{\link[=read_recipe]{read_recipe()}}

Other recipe serialization:
\code{\link[=read_recipe]{read_recipe()}}
}
\concept{recipe serialization}
