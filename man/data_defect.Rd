% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-defect.R
\name{data_defect}
\alias{data_defect}
\title{Data-defect diagnostics for a non-probability sample}
\usage{
data_defect(object, ddc_grid = c(0.001, 0.005, 0.01, 0.05, 0.1))
}
\arguments{
\item{object}{a prepped non-probability \code{weighting_spec} (from \code{\link[=prep]{prep()}}, built
with \code{weighting_spec(..., nonprob = TRUE)}).}

\item{ddc_grid}{the residual data-defect correlations to tabulate. Positive
values; only their magnitude matters.}
}
\value{
a list (class \code{weightflow_data_defect}) with the sample size \code{n}, the
estimated population size \code{N} (the sum of the final weights), the fraction \code{f},
the sensitivity \code{grid} (\code{ddc}, \code{n_eff}), and \code{aux}, a data frame of the
covariate-participation correlations (or \code{NULL} when the recipe has no
pseudo-weighting step).
}
\description{
For a non-probability sample, Meng (2018) decomposes the error of the sample
mean into the data-defect correlation (\code{rho}, the population correlation between
the target variable and the participation indicator), the sampling fraction and
the problem difficulty. The effective sample size a probability sample would need
to match the same mean-squared error is
\deqn{n_{\mathrm{eff}} = \frac{f/(1-f)}{\rho^2}, \qquad f = n/N,}
which does not depend on the outcome except through \code{rho}. Because \code{rho} on the
target variable is not observable from the sample alone, \code{data_defect()} returns
the effective size across a grid of plausible residual \code{rho} (read it as an
ignorance range, not a single number), plus the measurable selection strength on
the covariates used for pseudo-weighting (the largest correlation between an
auxiliary and participation, which pseudo-weighting neutralises).
}
\references{
Meng, X.-L. (2018). Statistical paradises and paradoxes in big data (I).
Annals of Applied Statistics 12(2), 685-726.
}
\seealso{
\code{\link[=step_pseudoweight]{step_pseudoweight()}}, \code{\link[=report_weighting]{report_weighting()}}
}
\concept{non-probability tools}
