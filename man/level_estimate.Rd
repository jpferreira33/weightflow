% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/variance-panel.R
\name{level_estimate}
\alias{level_estimate}
\alias{level_mean}
\alias{level_total}
\title{Level estimate for a single panel wave, with its replicate variance}
\usage{
level_estimate(
  wb,
  statistic,
  wave = NULL,
  level = 0.95,
  ci_type = c("normal", "t"),
  df = NULL
)

level_mean(wb, variable, wave = NULL, level = 0.95)

level_total(wb, variable, wave = NULL, level = 0.95)
}
\arguments{
\item{wb}{a \code{weightflow_wave_boot} or \code{weightflow_wave_jack}.}

\item{statistic}{a function \verb{function(w, data)} returning one number.}

\item{wave}{optional wave label (defaults to the first).}

\item{level}{confidence level for the normal interval.}

\item{ci_type}{\code{"normal"} (default, z) or \code{"t"} (Student t with \code{df} df).}

\item{df}{degrees of freedom for the \code{"t"} interval; \code{NULL} uses the object's design df.}

\item{variable}{name of a numeric variable in the wave's data.}
}
\value{
an object of class \code{weightflow_level} with \code{estimate}, \code{se}, \code{V} and the interval.
}
\description{
Estimates a statistic in ONE wave of a \code{\link[=wave_bootstrap]{wave_bootstrap()}} or \code{\link[=wave_jackknife]{wave_jackknife()}} object,
with the variance from that wave's replicates. Lets you report a level and a change from
the same coordinated object.
}
\seealso{
\code{\link[=change_estimate]{change_estimate()}}, \code{\link[=level_mean]{level_mean()}}, \code{\link[=level_total]{level_total()}}
}
