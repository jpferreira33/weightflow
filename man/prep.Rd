% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prep.R
\name{prep}
\alias{prep}
\title{Estimate the weighting cascade}
\usage{
prep(spec, min_cell_n = 30, max_factor = 2.5, warn = FALSE)
}
\arguments{
\item{spec}{a weighting_spec.}

\item{min_cell_n}{integer. Minimum number of cases per adjustment cell
(weighting class, poststratum). Cells below this raise a (non-fatal)
warning recommending collapsing or switching to raking. Default 30,
following Kalton and Flores-Cervantes (2003). Set to NULL to disable.}

\item{max_factor}{numeric. Adjustment factor above which a cell is flagged
as excessive. Default 2.5. Set to NULL to disable.}

\item{warn}{logical. If TRUE, the quality alerts are also raised as R
warnings during prep(). Default FALSE: alerts are always computed, stored
on the object (\verb{$alerts}) and shown in the HTML report, but not raised as
warnings, so they do not flood bootstrap/jackknife replicate fits.}
}
\value{
a "prepped_weighting_spec" object. Every quality incident is recorded
in \verb{$alerts} (readable with \code{\link[=weighting_alerts]{weighting_alerts()}} / \code{\link[=has_alerts]{has_alerts()}}),
regardless of \code{warn}. This includes warnings a step raises internally, such
as a calibration that could not meet its constraints: they are captured into
\verb{$alerts} even when the surrounding warnings are suppressed, so \verb{$alerts} is
the single reliable channel for programmatic quality control.
}
\description{
Runs an inert \code{\link[=weighting_spec]{weighting_spec()}} recipe. Starting from the design base
weights, \code{prep()} applies each step in the order it was piped, multiplying the
current weight by that step's adjustment factor, and returns an object holding
the weight at every stage, the per-step diagnostics and the quality alerts.
This is the only function that computes weights.
}
\examples{
rec <- weighting_spec(sample_survey, base_weights = pw) |>
  step_nonresponse(respondent = responded, method = "weighting_class", by = "region")
prep(rec)
}
