% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adjust-nr-sensitivity.R
\name{step_nr_sensitivity}
\alias{step_nr_sensitivity}
\title{Sensitivity of a mean to nonignorable nonresponse or selection}
\usage{
step_nr_sensitivity(
  spec,
  y,
  formula,
  respondent = NULL,
  phi = c(0, 0.25, 0.5, 0.75, 1),
  id = NULL
)
}
\arguments{
\item{spec}{a \code{weighting_spec}.}

\item{y}{the study variable (bare column name), observed for respondents and
\code{NA} for nonrespondents.}

\item{formula}{one-sided formula of the auxiliaries for the proxy, observed for
all units, e.g. \code{~ region + sex + age}.}

\item{respondent}{optional response/participation indicator (bare column or
condition). Defaults to \code{!is.na(y)}.}

\item{phi}{the sensitivity grid, values in \verb{[0, 1]}. \code{0} is MAR; Little et al.
(2020) suggest \code{0.5} as a central value; above \code{0.5} the implied mechanism is
often unrealistically strong.}

\item{id}{optional stable step id.}
}
\value{
the input \code{weighting_spec} with this diagnostic step appended.
}
\description{
A diagnostic step (it does not change any weight) that gauges how much the
weighted mean of a study variable could move if response, or participation in a
non-probability sample, depended on the outcome itself beyond the observed
auxiliaries. It implements the proxy pattern-mixture model of Andridge and Little
(2011): the auxiliaries are reduced to a single proxy (the respondent regression
prediction of \code{y}), and a single sensitivity parameter \code{phi} in \verb{[0, 1]} moves the
mechanism from ignorable given the proxy (\code{phi = 0}, MAR) to depending only on the
outcome (\code{phi = 1}). Evaluated over a grid of \code{phi}, the adjusted means form an
\emph{ignorance interval} to read alongside the sampling confidence interval; see
\code{\link[=nr_sensitivity]{nr_sensitivity()}} and the report block.
}
\details{
The proxy correlation \code{rho} (the multiple correlation of \code{y} on the auxiliaries
among respondents) sets how informative the auxiliaries are: a weak proxy widens
the ignorance interval (at \code{phi = 1} the slope is \code{1/rho}). The step reads the
base design weights, so place it anywhere in the recipe; it needs the
nonrespondents still present (a study variable that is \code{NA} for them, or an
explicit \code{respondent} indicator).
}
\references{
Andridge, R. R. and Little, R. J. A. (2011). Proxy pattern-mixture analysis for
survey nonresponse. Journal of Official Statistics 27(2), 153-180.
}
\seealso{
\code{\link[=nr_sensitivity]{nr_sensitivity()}}, \code{\link[=step_assert]{step_assert()}}

Other weighting steps:
\code{\link[=step_assert]{step_assert()}},
\code{\link[=step_calibrate]{step_calibrate()}},
\code{\link[=step_drop_ineligible]{step_drop_ineligible()}},
\code{\link[=step_model_calibration]{step_model_calibration()}},
\code{\link[=step_nonresponse]{step_nonresponse()}},
\code{\link[=step_pseudoweight]{step_pseudoweight()}},
\code{\link[=step_rescale]{step_rescale()}},
\code{\link[=step_round]{step_round()}},
\code{\link[=step_select_within]{step_select_within()}},
\code{\link[=step_trim]{step_trim()}},
\code{\link[=step_trim_calibrated]{step_trim_calibrated()}},
\code{\link[=step_trim_weights]{step_trim_weights()}},
\code{\link[=step_unknown_eligibility]{step_unknown_eligibility()}}
}
\concept{weighting steps}
