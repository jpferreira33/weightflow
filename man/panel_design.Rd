% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/panel-design.R
\name{panel_design}
\alias{panel_design}
\title{Describe the rotating-panel structure of a survey}
\usage{
panel_design(
  data,
  unit,
  wave,
  rotation_group = NULL,
  cluster = NULL,
  pattern = NULL,
  waves = NULL,
  reference_wave = NULL
)
}
\arguments{
\item{data}{a stacked \code{data.frame}, one row per unit per wave.}

\item{unit}{one or more column names (a character vector) that together
identify the longitudinal unit, stable across waves. A household is often a
single id (\code{"ID"}); a person needs several (\code{c("ID", "nper")} = household id
plus person line number). The columns are pasted into the tracking key.}

\item{wave}{string: the column holding the wave/period.}

\item{rotation_group}{optional string: the column holding the rotation group /
panel. Needed to derive \verb{Pr(panel selection)} exactly; without it that field
is left \code{NA} and only the unit-level overlap is computed.}

\item{cluster}{optional one or more column names identifying a within-unit
cluster (e.g. the household \code{"ID"}) when the tracked unit is a person but the
overlap is realised at the household level.}

\item{pattern}{optional string describing the rotation scheme, for verification
only: CEPAL's \code{"4(0)1"}, the CPS \code{"4-8-4"}, or a plain integer like \code{"6"}.
Nothing in the computation depends on parsing it.}

\item{waves}{optional character vector giving the wave order explicitly;
defaults to \code{sort(unique(data[[wave]]))}.}

\item{reference_wave}{optional wave whose population the longitudinal weight
represents (the calibration target for a longitudinal recipe); defaults to
the first wave. Read by \code{\link[=step_longitudinal]{step_longitudinal()}}.}
}
\value{
\code{data}, unchanged as a data frame, with the descriptor in
\code{attr(data, "wf_panel")} and class \code{"wf_panel_design"} prepended.
}
\description{
Tags stacked per-wave survey data with its rotation structure so the panel
steps and the report can read it, and so the overlap and linkage quality can
be inspected \emph{before} any weighting. \code{panel_design()} computes only; it does
not create or change weights. It is the panel analogue of \code{\link[=reference_sample]{reference_sample()}}:
the descriptor lives in \code{attr(data, "wf_panel")} and the returned object is
still an ordinary \code{data.frame}.
}
\details{
\code{data} must be in \strong{stacked (long) form}: one row per unit per wave, with a
\code{unit} column that is stable across waves and a \code{wave} column identifying the
period. From the \code{unit} x \code{wave} membership it derives the observed overlap
matrix (the fraction of each wave retained in every other wave, i.e. CEPAL's
\emph{traslape}), and, when \code{rotation_group} is given, the panel-selection
probability \verb{Pr(panel selection)} for each adjacent pair and for the full
combination -- the reciprocal of which is the CEPAL panel base-weight factor.

The linkage quality is measured, not assumed: if a \code{pattern} is supplied the
observed adjacent overlap is compared against the one it implies, and a large
gap (alert \code{PN-01}) is the early warning that the linkage key is unstable
(relabelled ids, a redesigned frame, duplicated panels). Uneven rotation-group
sizes, which break the scalar reciprocal of \verb{Pr(panel selection)}, raise
\code{PN-02}.
}
\examples{
# person-level tracking: the key is household id + person line number
pd <- panel_design(panel_ine, unit = c("id_hogar", "nper"), wave = "ola",
                   rotation_group = "grupo_rotacion", cluster = "id_hogar",
                   pattern = "6")
pd            # overlap matrix, Pr(panel selection), linkage rate, alerts
summary(pd)
}
\seealso{
\code{\link[=panel_merge]{panel_merge()}}, \code{\link[=reference_sample]{reference_sample()}}
}
