% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimation-spec.R
\name{step_domain}
\alias{step_domain}
\alias{step_filter}
\alias{step_estimate}
\alias{step_transition}
\title{Declarative estimation over a coordinated panel object}
\usage{
step_domain(x, ...)

step_filter(x, condition)

step_estimate(
  x,
  statistic,
  over = NULL,
  type = c("absolute", "relative"),
  waves = NULL,
  contrast = NULL,
  level = 0.95,
  label = NULL
)

step_transition(x, from, to, format = c("row", "col", "joint", "counts"))
}
\arguments{
\item{x}{a \code{weightflow_wave_boot} / \code{weightflow_wave_jack}, or a \code{weightflow_estimation}
to extend.}

\item{...}{for \code{step_domain()}, one or more grouping columns (unquoted or as strings);
they stack, so the disaggregation is their cross (e.g. region x sex).}

\item{condition}{for \code{step_filter()}, a logical expression (unquoted) that selects the
subpopulation to estimate over -- e.g. \code{edad >= 25 & edad <= 54}. It is evaluated in
each wave's data; rows are \strong{masked} (not dropped), so the coordinated replicate
structure and the overlap covariance are preserved. Multiple \code{step_filter()} calls
stack (their conditions are ANDed).}

\item{statistic}{the estimand, as a DSL call -- \code{mean(var)}, \code{total(var)}, \code{prop(cond)},
\code{ratio(num, den)}, \code{quantile(var, p)} -- or a \verb{function(w, data)}.}

\item{over}{what to estimate: \code{"change"} (between two waves), \code{"level"} (one wave) or
\code{"contrast"} (a linear combination, with \verb{contrast=}). Default: \code{"change"} if the object
has >= 2 waves, else \code{"level"}; \code{"contrast"} is implied when \verb{contrast=} is given.}

\item{type}{for \code{over = "change"}, \code{"absolute"} (default) or \code{"relative"} (\code{theta2/theta1 - 1}).}

\item{waves}{optional wave label(s): one for \code{"level"}, two for \code{"change"}.}

\item{contrast}{numeric weights (one per wave) for \code{over = "contrast"}.}

\item{level}{confidence level.}

\item{label}{optional name for the estimand (defaults to the statistic's expression).}

\item{from, to}{for \code{step_transition()}, the from-/to-state columns.}

\item{format}{transition table format (\code{"row"}, \code{"col"}, \code{"joint"}, \code{"counts"}).}
}
\value{
a \code{weightflow_estimation}.
}
\description{
\code{step_domain()} and \code{step_estimate()} build an estimation pipeline (\code{weightflow_estimation})
on top of a saved \code{\link[=wave_bootstrap]{wave_bootstrap()}} / \code{\link[=wave_jackknife]{wave_jackknife()}} object, so that changes, levels
and linear contrasts -- with the honest overlap covariance -- can be requested declaratively
and disaggregated, the way the weighting recipe is built with \verb{step_*}. It is a thin front
end over \code{\link[=change_estimate]{change_estimate()}}, \code{\link[=panel_estimate]{panel_estimate()}} and \code{\link[=level_estimate]{level_estimate()}}; the coordinated
replicates do the variance. Evaluate with \code{\link[=collect_estimates]{collect_estimates()}} (printing does it too).
}
\examples{
t1 <- subset(panel_ine, ola == 1 & disp == "R")
t2 <- subset(panel_ine, ola == 2 & disp == "R")
wb <- wave_bootstrap(
  list(T1 = weighting_spec(t1, base_weights = w_base),
       T2 = weighting_spec(t2, base_weights = w_base)),
  replicates = 100, strata = "estrato", psu = "psu", seed = 1, progress = FALSE)

# net change of the unemployment rate, by region
collect_estimates(wb |> step_domain(region) |>
  step_estimate(mean(desocupado), over = "change"))

# subpopulation: same change among the working-age population (rows masked, not dropped)
collect_estimates(wb |> step_filter(edad >= 25 & edad <= 54) |>
  step_estimate(mean(desocupado), over = "change"))
}
\seealso{
\code{\link[=collect_estimates]{collect_estimates()}}, \code{\link[=change_estimate]{change_estimate()}}, \code{\link[=panel_estimate]{panel_estimate()}}, \code{\link[=level_estimate]{level_estimate()}}
}
