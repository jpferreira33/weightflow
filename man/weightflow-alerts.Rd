% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/weightflow-alerts.R
\name{weightflow-alerts}
\alias{weightflow-alerts}
\title{Quality alerts raised while preparing a recipe}
\description{
\code{\link[=prep]{prep()}} computes non-fatal \emph{quality alerts} at every stage of the cascade,
not only at the end. Each alert names a specific risk, its trigger and a
remedy. Alerts are always stored on the prepped object (read them with
\code{\link[=weighting_alerts]{weighting_alerts()}} / \code{\link[=has_alerts]{has_alerts()}}) and shown in the HTML report; with
\code{prep(warn = TRUE)} they are also raised as R warnings. Every alert is tagged
with the step that produced it, e.g. \code{"[step_calibrate] ..."}.
}
\details{
This page catalogues the alerts by theme. Thresholds are the \code{\link[=prep]{prep()}} defaults
unless noted; \code{min_cell_n} and \code{max_factor} are arguments of \code{\link[=prep]{prep()}}.
}
\section{Weight distribution}{

\itemize{
\item \emph{Negative calibration weights}: an unbounded linear/GREG calibration
produced weight(s) below zero. They stay active (the totals are honest), but
a negative survey weight is rarely wanted and makes the estimator erratic.
Use a bounded distance (\code{calfun = "logit"} with \code{bounds}) or
\code{\link[=step_trim_calibrated]{step_trim_calibrated()}}.
\item \emph{Sub-one weights}: unit(s) with a final weight below 1, so they represent
less than themselves. Usually a sign of an over-aggressive adjustment.
\item \emph{Extreme g-factors}: a calibration adjustment factor outside the
Deville-Sarndal range \verb{[0.1, 10]}. Consider bounds, a ridge \code{penalty}, or
coarser auxiliaries.
}
}

\section{Adjustment cells}{

\itemize{
\item \emph{Small cells}: an adjustment cell (weighting class or post-stratum) with
fewer than \code{min_cell_n} (default 30, Kalton and Flores-Cervantes 2003)
cases. Collapse cells or switch to raking.
\item \emph{Empty cells}: a cell with no unit to adjust to (no respondents, or all of
unknown eligibility); the affected units are set to weight 0.
\item \emph{Large adjustment factors}: a per-cell factor above \code{max_factor} (default
2.5), which inflates the variance.
}
}

\section{Nonresponse and response propensities}{

\itemize{
\item \emph{Tiny propensities}: a minimum fitted response propensity below 0.01 blows
up the \code{1/p} weights; check the model or trim.
\item \emph{Collapsed propensity classes}: the fitted propensities were nearly
constant, so the requested \code{num_classes} could not be formed and the class
correction did nothing.
\item \emph{Miscalibrated propensities}: a calibration slope far from 1; honest
probabilities matter for \code{1/p}. Use \code{num_classes} (rank-robust) or revise
the model.
\item \emph{Non-positive nonresponse g-weights}: an implied response probability of
zero or below; use a bounded distance or other auxiliaries.
}
}

\section{Calibration}{

\itemize{
\item \emph{Ill-conditioned system}: near-collinear auxiliaries (large condition
number) make the weights unstable; drop a redundant auxiliary or set a ridge
\code{penalty}.
\item \emph{Did not converge}: raking, linear or bounded calibration stopped without
meeting every target; increase \code{maxit} or check the margins are mutually
consistent.
\item \emph{Reconciled control totals}: the tidy \code{totals} did not all sum to the same
population size and were rescaled to a common N (informative, not an error).
}
}

\section{Machine-learning adjustments}{

\itemize{
\item \emph{Learner without cross-fitting}: a flexible learner (tree / forest / boost)
run without \code{crossfit} can understate the variance through same-sample
prediction. Add \code{crossfit} to estimate each unit out of sample.
}
}

\section{Finalising}{

\itemize{
\item \emph{Rounded to zero}: rounding pushed a small or negative weight to exactly 0,
so the unit left the active set and \code{\link[=collect_weights]{collect_weights()}}; round to more
decimals or resolve those weights first.
}
}

\seealso{
\code{\link[=prep]{prep()}}, \code{\link[=weighting_alerts]{weighting_alerts()}}, \code{\link[=has_alerts]{has_alerts()}}, \code{\link[=step_assert]{step_assert()}} for a
hard quality gate, and \code{vignette("inspecting-auditing")} for the full
programmatic quality-control workflow.
}
