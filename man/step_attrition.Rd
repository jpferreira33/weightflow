% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spec-steps-panel.R
\name{step_attrition}
\alias{step_attrition}
\title{Attrition adjustment for panel waves}
\usage{
step_attrition(
  spec,
  respondent,
  method = c("propensity", "rhg", "weighting_class", "calibration"),
  formula = NULL,
  by = NULL,
  engine = c("logit", "tree", "forest", "boost"),
  num_classes = 5L,
  id = NULL
)
}
\arguments{
\item{spec}{a weighting_spec.}

\item{respondent}{an unquoted 0/1 column or logical condition, TRUE for the units that
responded in the wave being adjusted (e.g. \code{disp_T2 == "R"}).}

\item{method}{attrition estimator: \code{"propensity"} (individual \code{1/phi}, the SLID/ECLAC
response-propensity weighting; default), \code{"rhg"} (response homogeneity groups: propensity
stratified into \code{num_classes} classes), \code{"weighting_class"} (design-variable cells), or
\code{"calibration"} (Sarndal-Lundstrom). (\code{"hazard"} -- multi-wave retention chaining -- and
the SLID/ECLAC fallback imputations are added next.)}

\item{formula}{model formula for \code{"propensity"}/\code{"rhg"}, in covariates observed for
responders and nonrespondents (from a wave where the unit was seen).}

\item{by}{adjustment cells for \code{"weighting_class"}.}

\item{engine}{propensity engine (\code{"logit"}/\code{"tree"}/\code{"forest"}/\code{"boost"}).}

\item{num_classes}{number of propensity classes for \code{"rhg"}.}

\item{id}{optional stable step id.}
}
\value{
the \code{weighting_spec} with the attrition step appended.
}
\description{
The panel-facing nonresponse step: it adjusts for \strong{attrition} (nonresponse between
waves) when building a longitudinal weight, reweighting the units that stayed in to also
represent those that dropped out, among the units that remain \strong{eligible}. It is a thin
wrapper over \code{\link[=step_nonresponse]{step_nonresponse()}} -- so the theory stays visible in the recipe -- that
uses the same estimators but under a name that reads correctly in a panel cascade and with
the panel conventions: the covariates should come from a wave where the unit was observed
(e.g. the first period), and it does NOT absorb eligibility (out-of-scope and
unknown-eligibility go in \code{\link[=step_drop_ineligible]{step_drop_ineligible()}} / \code{\link[=step_unknown_eligibility]{step_unknown_eligibility()}}).
}
\details{
The attrition adjustment prescribed by ECLAC's household-survey manual (ch. XVI) and used by
Statistics Canada's SLID (Naud 2002; LaRoche 2003) is \strong{response-propensity weighting}
(\code{method = "propensity"}, i.e. inverse of the estimated response probability; Little 1986;
Rosenbaum 1987) -- not an ECLAC invention. \code{method = "rhg"} is the response-homogeneity-group
variant (propensity stratified into \code{num_classes} groups, then the class-mean rate), which
stabilises the weights.
}
\examples{
\donttest{
wide <- panel_merge(
  list(T1 = subset(panel_ine, ola == 1), T2 = subset(panel_ine, ola == 2)),
  by = c("id_hogar", "nper"), require = "all")
weighting_spec(wide, base_weights = w_base_T1) |>
  step_panel_overlap(prob = 5 / 6) |>
  step_drop_ineligible(disp_T2 == "OS", reason = "left the target population") |>
  step_attrition(respondent = disp_T2 == "R", method = "propensity",
                 formula = ~ edad_T1 + sexo_T1 + region_T1) |>
  prep()
}
}
\seealso{
\code{\link[=step_nonresponse]{step_nonresponse()}}, \code{\link[=step_panel_overlap]{step_panel_overlap()}}, \code{\link[=step_drop_ineligible]{step_drop_ineligible()}}
}
