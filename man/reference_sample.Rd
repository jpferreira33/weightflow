% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spec-steps-final.R
\name{reference_sample}
\alias{reference_sample}
\title{Use a weighted survey as the calibration reference instead of a frame}
\usage{
reference_sample(data, weights, replicates = NULL)
}
\arguments{
\item{data}{a \code{data.frame} of reference-survey microdata, with the columns
used in \code{x_formula} and the model predictors.}

\item{weights}{either the name (string) of a positive weight column in \code{data},
or a numeric vector with one weight per row.}

\item{replicates}{optional numeric matrix (or data.frame) of replicate weights
for the reference survey -- one row per reference unit, one column per
replicate -- used to propagate the reference sampling variance through
\code{\link[=bootstrap_weights]{bootstrap_weights()}}. \code{NULL} (default) treats the totals as fixed. Note that
only \code{\link[=bootstrap_weights]{bootstrap_weights()}} pairs the reference replicates and propagates this
variance; \code{\link[=jackknife_weights]{jackknife_weights()}} treats the estimated totals as fixed even when
\code{replicates} is supplied, so use the bootstrap when this component matters.}
}
\value{
\code{data} tagged so that \code{step_model_calibration()} weights its totals by
\code{weights}. It is still an ordinary \code{data.frame}.
}
\description{
Wraps a reference-survey microdata \code{data.frame} together with its design
weights so it can be passed as the \code{population} argument of
\code{\link[=step_model_calibration]{step_model_calibration()}} (and any step that takes a \code{population} frame).
The calibration totals are then the \emph{weighted} sums over the reference survey
-- an estimate of the population totals -- instead of unweighted sums over a
full frame. This is the model-assisted / two-survey setup: fit the model on
your sample, project it onto a larger reference survey, and calibrate to the
weighted totals of the projection (Wu and Sitter 2001; Kim and Rao 2012).
}
\details{
A reference survey with all weights equal to 1 reproduces the plain-frame
behaviour exactly. To propagate the reference survey's own sampling variance
into the recipe-aware bootstrap, pass its replicate weights through
\code{replicates}: each bootstrap replicate then re-estimates the totals from the
paired reference replicate (Opsomer and Erciulescu 2021), so the extra
variance from estimating the totals is captured. Without \code{replicates} the
totals are treated as fixed (a reasonable approximation when the reference is
much larger than the sample, and the same assumption made when calibrating to
another survey's published totals).
}
\examples{
ref <- reference_sample(population, weights = rep(1, nrow(population)))
}
\seealso{
\code{\link[=step_model_calibration]{step_model_calibration()}}
}
