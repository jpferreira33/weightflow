% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/step_pseudo_weight.R
\name{step_pseudo_weight}
\alias{step_pseudo_weight}
\title{Pseudo-weights for a nonprobability sample (inverse propensity)}
\usage{
step_pseudo_weight(
  spec,
  reference,
  reference_weights,
  formula,
  pooling = c("unweighted_nps", "weighted", "pseudo_lik"),
  engine = c("logit", "tree", "forest", "boost"),
  num_classes = NULL,
  crossfit = NULL,
  crossfit_seed = NULL
)
}
\arguments{
\item{spec}{a weighting_spec built on the nonprobability sample.}

\item{reference}{a data frame: the probability reference sample, with the
covariates in \code{formula} and a design-weight column.}

\item{reference_weights}{name of the design-weight column in \code{reference}.}

\item{formula}{one-sided formula of the common covariates, e.g.
\code{~ region + sex + age}.}

\item{pooling}{how the samples' weights enter the propensity model:
"unweighted_nps" (default), "weighted", or "pseudo_lik". See Details.}

\item{engine}{propensity engine: "logit", "tree", "forest" or "boost".}

\item{num_classes}{optional integer; if given, smooth the pseudo-weights into
that many propensity classes instead of using 1/p per unit.}

\item{crossfit}{optional integer K >= 2 for K-fold cross-fitting of the
propensity (recommended with flexible engines).}

\item{crossfit_seed}{optional seed for the cross-fitting folds.}
}
\value{
The spec with the step appended (evaluated later by \code{prep()}).
}
\description{
Adds a step that derives inverse-propensity pseudo-weights for a
nonprobability sample, using a probability \strong{reference} sample to anchor the
estimates to the population. It is meant as the first step of a recipe built
on the nonprobability sample (which has no design weights of its own); the
resulting pseudo-weights can then be calibrated, trimmed, etc. like any other
weights.
}
\details{
The nonprobability sample and the reference are stacked, a participation
propensity \eqn{P(\text{in NPS} \mid x)} is fitted on the pooled data with the
chosen engine, and each nonprobability unit gets a pseudo-weight proportional
to the inverse of its estimated propensity. The reference design weights are
always used so that the propensity targets the population rather than the
(unweighted) reference.
}
