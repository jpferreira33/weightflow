% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/variance-panel.R
\name{panel_estimate}
\alias{panel_estimate}
\alias{panel_mean}
\alias{panel_total}
\title{Linear combination of panel waves, with honest between-wave variance}
\usage{
panel_estimate(
  wb,
  statistic,
  contrast = NULL,
  waves = NULL,
  level = 0.95,
  ci_type = c("normal", "t"),
  df = NULL
)

panel_mean(wb, variable, contrast = NULL, waves = NULL, level = 0.95)

panel_total(wb, variable, contrast = NULL, waves = NULL, level = 0.95)
}
\arguments{
\item{wb}{a \code{weightflow_wave_boot} from \code{\link[=wave_bootstrap]{wave_bootstrap()}} or a \code{weightflow_wave_jack}
from \code{\link[=wave_jackknife]{wave_jackknife()}}.}

\item{statistic}{a function \verb{function(w, data)} returning one number, evaluated per wave.}

\item{contrast}{numeric weights, one per wave, defining the combination; defaults to the
equal-weight average \code{rep(1/W, W)}. May be named by wave label (matched to \code{waves}).}

\item{waves}{optional character vector of the waves to combine (defaults to all, in order).}

\item{level}{confidence level for the normal-approximation interval.}

\item{ci_type}{\code{"normal"} (default, z) or \code{"t"} (Student t with \code{df} df).}

\item{df}{degrees of freedom for the \code{"t"} interval; \code{NULL} uses the object's design df.}

\item{variable}{name of a numeric variable present in every wave.}
}
\value{
an object of class \code{weightflow_panel_estimate} with \code{estimate}, \code{se}, \code{V}, \code{Vind}
(the independence-assuming variance), \code{deff = V / Vind}, the covariance matrix \code{Sigma},
the per-wave \code{point} estimates, the \code{contrast}, and the confidence interval.
}
\description{
Estimates any linear combination \code{psi = sum(contrast * theta_wave)} of a statistic across
panel waves -- an annual average (\code{contrast = rep(1/W, W)}), a net change
(\code{contrast = c(-1, 1)}, i.e. what \code{\link[=change_estimate]{change_estimate()}} does), a semester contrast, etc. --
and its variance \verb{a' Sigma a}, where \code{Sigma} is the between-wave covariance matrix that the
overlap of a rotating panel induces. That covariance is taken straight from the coordinated
replicates of \code{\link[=wave_bootstrap]{wave_bootstrap()}} or \code{\link[=wave_jackknife]{wave_jackknife()}}, so it is correct without any
analytic covariance formula. Treating the waves as independent (the diagonal of \code{Sigma}
only) understates the variance of an average and overstates that of a change.
}
\examples{
\donttest{
waves <- lapply(1:3, function(t)
  weighting_spec(subset(panel_ine, ola == t & disp == "R"), base_weights = w_base))
names(waves) <- c("T1", "T2", "T3")
wb <- wave_bootstrap(waves, replicates = 100, strata = "estrato", psu = "psu",
                     seed = 1, progress = FALSE)
rate <- function(w, d) weighted.mean(d$desocupado, w, na.rm = TRUE)
panel_estimate(wb, rate)                       # average unemployment level over the waves
panel_estimate(wb, rate, contrast = c(-1, 0, 1))  # T3 - T1 change
}
}
\seealso{
\code{\link[=change_estimate]{change_estimate()}}, \code{\link[=wave_bootstrap]{wave_bootstrap()}}, \code{\link[=wave_jackknife]{wave_jackknife()}}
}
