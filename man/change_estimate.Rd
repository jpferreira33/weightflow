% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/variance-panel.R
\name{change_estimate}
\alias{change_estimate}
\alias{change_mean}
\alias{change_total}
\title{Net change between two panel waves, with honest variance}
\usage{
change_estimate(
  wb,
  statistic,
  waves = NULL,
  level = 0.95,
  type = c("absolute", "relative"),
  by = NULL,
  ci_type = c("normal", "t", "percentile"),
  df = NULL
)

change_mean(
  wb,
  variable,
  waves = NULL,
  level = 0.95,
  type = c("absolute", "relative"),
  by = NULL
)

change_total(
  wb,
  variable,
  waves = NULL,
  level = 0.95,
  type = c("absolute", "relative"),
  by = NULL
)
}
\arguments{
\item{wb}{a \code{weightflow_wave_boot} from \code{\link[=wave_bootstrap]{wave_bootstrap()}}.}

\item{statistic}{a function \verb{function(w, data)} returning a single number, evaluated on
each wave's weights and data (e.g. \code{function(w, d) weighted.mean(d$y, w)}).}

\item{waves}{optional length-2 character vector naming the two waves (defaults to the
first two).}

\item{level}{confidence level for the interval.}

\item{type}{\code{"absolute"} (default) for the net change \code{theta2 - theta1}, or \code{"relative"}
for the relative change \code{theta2 / theta1 - 1} (e.g. the percent change of a rate). The
relative change's variance comes from the coordinated replicate distribution of the
ratio (bootstrap) or the delta method on the level pieces (jackknife), so the overlap
covariance is captured either way. \code{V1}, \code{V2}, \code{cov}, \code{rho} always describe the levels.}

\item{by}{optional name of a grouping column present in both waves: returns one change
per domain (a \code{weightflow_change_by} table) instead of a single change.}

\item{ci_type}{interval type: \code{"normal"} (default, z-based), \code{"t"} (Student t with \code{df}
degrees of freedom, wider and safer with few PSUs) or \code{"percentile"} (bootstrap only).}

\item{df}{degrees of freedom for the \code{"t"} interval; \code{NULL} (default) uses the design df
stored on the object (total PSUs minus strata).}

\item{variable}{name of a numeric variable present in both waves.}
}
\value{
an object of class \code{weightflow_change} with the estimate, \code{se}, \code{V}, \code{V1}, \code{V2},
\code{Vind = V1 + V2}, \code{cov}, \code{rho}, \code{deff_change} and the confidence interval; or, with
\verb{by=}, a \code{weightflow_change_by} holding one row per domain.
}
\description{
Estimates a net change (a statistic in wave 2 minus the same statistic in wave 1) from a
\code{\link[=wave_bootstrap]{wave_bootstrap()}}, decomposing its variance into \code{V1}, \code{V2} and the sampling covariance
the overlap induces: \code{V = V1 + V2 - 2*Cov}. Because the bootstrap replicates are
coordinated by PSU, \code{Cov} emerges from them -- no analytic covariance formula. Reports
the correlation \code{rho} and \code{deff_change = V / (V1 + V2)}, i.e. how much the overlap
lowered the variance of the change relative to treating the waves as independent.
}
\seealso{
\code{\link[=wave_bootstrap]{wave_bootstrap()}}, \code{\link[=change_mean]{change_mean()}}, \code{\link[=change_total]{change_total()}}, \code{\link[=level_estimate]{level_estimate()}}
}
