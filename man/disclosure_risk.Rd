% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/disclosure-risk.R
\name{disclosure_risk}
\alias{disclosure_risk}
\title{Flag re-identification risk from outlier weights within a publication cell}
\usage{
disclosure_risk(object, by, ratio = 10)
}
\arguments{
\item{object}{a prepped \code{weighting_spec} (the output of \code{\link[=prep]{prep()}}).}

\item{by}{the name(s) of the publication cell column(s) (e.g. \code{"region"}, or
\code{c("region", "sex")}). The risk is judged within each cell.}

\item{ratio}{the multiple of the cell median weight above which a unit is
flagged. Default 10.}
}
\value{
A \code{data.frame}, one row per flagged unit, with the row index (\code{.row}),
the \code{cell}, the unit \code{weight}, the \code{cell_median}, the \code{cell_n} (active units in
the cell) and \code{cell_share} (the unit's fraction of the cell's total weight),
ordered from the largest weight down. Zero rows when nothing is flagged.
}
\description{
A unit whose final weight is far larger than the rest of its publication cell is
a disclosure risk in a public-use file: an extreme weight makes a rare unit
stand out. \code{disclosure_risk()} flags, within each cell defined by \code{by}, the units
whose final weight exceeds \code{ratio} times the cell's median weight, and reports
the unit's share of the cell's total weight. Trimming (\code{step_trim_weights()} or
the totals-preserving \code{step_trim_calibrated()}) is the usual remedy.
}
\examples{
fit <- weighting_spec(sample_survey, base_weights = pw) |>
  step_nonresponse(respondent = responded, method = "weighting_class", by = "region") |>
  prep()
disclosure_risk(fit, by = "region")
}
\seealso{
\code{\link[=step_trim_weights]{step_trim_weights()}}, \code{\link[=step_trim_calibrated]{step_trim_calibrated()}},
\code{\link[=collect_replicate_weights]{collect_replicate_weights()}}
}
\concept{confidentiality tools}
