% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wave-chain.R
\name{wave_contrast}
\alias{wave_contrast}
\title{Linear combination of an estimand across a chain of periods}
\usage{
wave_contrast(carries, estimand, contrast = NULL, level = 0.95)
}
\arguments{
\item{carries}{a list of \code{wf_wave_carry} objects, one per period entering the combination.
Order defines the order of \code{contrast}; they are used as given (not sorted).}

\item{estimand}{name of the estimand to combine, as it appears in \code{carry$point} (a domain
estimand is named e.g. \code{"rate|sex=F"}).}

\item{contrast}{numeric weights, one per carry. Defaults to the average \code{rep(1/W, W)}.}

\item{level}{confidence level for the interval.}
}
\value{
a one-row \code{data.frame} with \code{estimate}, \code{se}, \code{V}, \code{lo}, \code{hi}, \code{R_used}, the periods
and the contrast used, plus the covariance matrix \code{Sigma} as an attribute.
}
\description{
\code{\link[=wave_step]{wave_step()}} reports the level of its own period and the net change against each carry it
was given -- that is, \strong{pairwise} contrasts. Many published series are not pairwise: a
rolling quarter is the average of three consecutive months, a semester-on-semester contrast
is \code{c(-1/2, -1/2, 1/2, 1/2)}, an annual average is \code{rep(1/W, W)}. \code{wave_contrast()} estimates
any such combination \eqn{\psi = a'\theta} directly from the carries the chain already wrote
to disk, with \eqn{V(\psi) = a' \Sigma a}.
}
\details{
The carries make this possible without keeping the waves in memory: each one stores the \code{R}
replicate values of every declared estimand, and those replicates are \strong{paired across
periods} because the coordination transferred the PSU multiplicities. Stacking them into an
\verb{R x W} matrix and taking its (uncentred-at-\code{R}) covariance recovers the full between-period
covariance matrix, from which any linear combination follows. With \code{contrast = c(-1, 1)} the
result reproduces the \verb{$change} row of \code{\link[=wave_step]{wave_step()}} exactly.
}
\seealso{
\code{\link[=wave_step]{wave_step()}}, \code{\link[=wave_carry]{wave_carry()}}; \code{\link[=panel_estimate]{panel_estimate()}} does the same on a
\code{\link[=wave_bootstrap]{wave_bootstrap()}} object, when all waves are held together.
}
