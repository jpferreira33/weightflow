% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/collect-step-detail.R
\name{collect_step_detail}
\alias{collect_step_detail}
\title{Per-unit detail of one step of the cascade}
\usage{
collect_step_detail(object, step = NULL)
}
\arguments{
\item{object}{a prepped \code{weighting_spec} (the output of \code{\link[=prep]{prep()}}).}

\item{step}{optional integer, which step to inspect (1 for the first piped
step). If \code{NULL} (default): a single step exposing native detail is used; if
several do, or if none do and the recipe has more than one step, an error
lists the steps so you can choose.}
}
\value{
The sample \code{data.frame} with \code{.weight_in} (the weight reaching the
step, carrying every earlier adjustment) and \code{.factor} (the multiplier the
step applied to each unit, \code{NA} where the incoming weight is zero) appended,
plus any native columns of the chosen step (for a propensity step:
\code{.propensity}, \code{.responded}, and \code{.class} when propensity classes are used),
which are \code{NA} outside the units the step covers.
}
\description{
A generic companion to \code{\link[=collect_weights]{collect_weights()}} that returns, for a single step, the
weight it received and the multiplier it applied to every unit, plus any
quantities that step computed internally (for a propensity step, the fitted
propensity and its class). \code{.weight_in} and \code{.factor} are read from the
stage-by-stage weights that \code{\link[=prep]{prep()}} already stores, so
\code{.weight_in * .factor} equals the weight leaving the step by construction,
for any step. Native columns (those a step exposes on its own) are \code{NA} for
units the step did not touch.
}
\examples{
fit <- weighting_spec(sample_survey, base_weights = pw) |>
  step_nonresponse(respondent = responded, method = "propensity",
                   formula = ~ sex + region, engine = "logit") |>
  prep()
d <- collect_step_detail(fit, step = 1)
head(d[!is.na(d$.factor), c(".weight_in", ".factor", ".propensity")])
}
\seealso{
\code{\link[=collect_weights]{collect_weights()}}, \code{\link[=collect_propensities]{collect_propensities()}}, \code{\link[=weight_factors]{weight_factors()}}
}
