% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prep.R
\name{weighting_alerts}
\alias{weighting_alerts}
\alias{has_alerts}
\title{Quality alerts recorded while preparing a recipe}
\usage{
weighting_alerts(object)

has_alerts(object)
}
\arguments{
\item{object}{a prepped_weighting_spec, as returned by \code{\link[=prep]{prep()}}.}
}
\value{
\code{weighting_alerts()}: a character vector (empty if the recipe ran
clean). \code{has_alerts()}: a single logical.
}
\description{
\code{weighting_alerts()} returns the character vector of quality incidents
recorded by \code{\link[=prep]{prep()}}, each tagged with the step that produced it.
\code{has_alerts()} is a convenience predicate. Every incident is captured here,
including warnings a step raises internally (for example a calibration that
could not meet its constraints), so this is the reliable channel for
programmatic quality control even when warnings were suppressed.
}
\examples{
fit <- weighting_spec(sample_survey, base_weights = pw) |>
  step_trim(max_ratio = 3) |>
  prep()
weighting_alerts(fit)
has_alerts(fit)
}
