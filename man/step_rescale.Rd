% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spec.R
\name{step_rescale}
\alias{step_rescale}
\title{Rescale (normalize) the weights}
\usage{
step_rescale(spec, to = c("n", "total"), total = NULL, by = NULL)
}
\arguments{
\item{spec}{a weighting_spec.}

\item{to}{"n" (weights sum to the number of active units, i.e. mean weight 1)
or "total" (weights sum to \code{total}).}

\item{total}{numeric. Target sum when to = "total".}

\item{by}{character. Rescale within these groups (optional). With to = "n",
each group sums to its own active count.}
}
\description{
Rescale (normalize) the weights
}
\examples{
weighting_spec(sample_survey, base_weights = pw) |>
  step_rescale(to = "n") |> prep()
}
