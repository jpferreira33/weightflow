% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transitions.R
\name{transition_matrix}
\alias{transition_matrix}
\title{Gross-flow transition matrix between two panel waves}
\usage{
transition_matrix(
  data,
  from,
  to,
  weights = NULL,
  states = NULL,
  format = c("row", "col", "joint", "counts")
)
}
\arguments{
\item{data}{a \code{data.frame} (wide longitudinal file) or a prepped \code{weighting_spec}.}

\item{from, to}{names of the earlier- and later-wave state columns.}

\item{weights}{a weight column name or a numeric vector; defaults to 1 (or the prepped
weight when \code{data} is a prepped spec).}

\item{states}{optional character vector fixing the state levels and their order.}

\item{format}{\code{"row"} (default, P(to|from)), \code{"col"} (P(from|to)), \code{"joint"} (P(from,to)) or
\code{"counts"} (weighted counts).}
}
\value{
a \code{weightflow_transition} object holding the matrix.
}
\description{
Weighted cross-tabulation of a categorical state at an earlier wave (\code{from}) against a later
wave (\code{to}) -- e.g. the labour-force status of the same people across two periods -- computed
on the \strong{longitudinal} weight. This is the gross flow (CEPAL ch. XVII): who moved between
which states. \code{\link[=boot_transition]{boot_transition()}} adds a per-cell standard error from the ordinary bootstrap.
}
\seealso{
\code{\link[=boot_transition]{boot_transition()}}
}
