% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/variance.R
\name{collect_replicate_weights}
\alias{collect_replicate_weights}
\title{Collect replicate weights into a data frame ready for srvyr}
\usage{
collect_replicate_weights(
  object,
  weight_name = ".weight",
  prefix = "rep_",
  drop_zero = TRUE
)
}
\arguments{
\item{object}{a \code{weightflow_boot} or \code{weightflow_jack} object.}

\item{weight_name}{name of the point-weight column to add.}

\item{prefix}{prefix for the replicate-weight columns (\code{rep_1}, \code{rep_2}, ...).}

\item{drop_zero}{keep only active units (point weight > 0).}
}
\value{
A data frame: the original columns, \code{weight_name}, and one column per
replicate. The number of replicates is in attribute \code{"R"}, and the
replication design in attributes \code{"type"}, \code{"scale"} and \code{"rscales"}.
}
\description{
Returns the data with the point weight and the replicate weights as columns,
so it can be fed directly to \code{srvyr::as_survey_rep()} (or
\code{survey::svrepdesign()}). Works for both a bootstrap (\code{weightflow_boot}) and a
delete-a-PSU jackknife (\code{weightflow_jack}). Replicate columns are full
(combined) weights, so use \code{combined.weights = TRUE} and \code{mse = TRUE}; the
correct \code{type}, \code{scale} and \code{rscales} for the object are attached as
attributes (\code{"type"}, \code{"scale"}, \code{"rscales"}) -- for a bootstrap
\code{scale = 1 / R}, \code{rscales = 1}; for the jackknife \code{scale = 1},
\code{rscales = (n_h - 1)/n_h} per replicate.
}
\examples{
spec <- weighting_spec(sample_survey, base_weights = pw) |>
  step_calibrate(method = "raking",
                 margins = list(region = c(table(population$region))))
boot <- bootstrap_weights(spec, replicates = 30, strata = "region",
                          psu = "psu", seed = 1, progress = FALSE)
df <- collect_replicate_weights(boot)   # or a weightflow_jack object
\donttest{
if (requireNamespace("srvyr", quietly = TRUE) &&
    requireNamespace("dplyr", quietly = TRUE)) {
  srvyr::as_survey_rep(df, weights = .weight,
                       repweights = dplyr::starts_with("rep_"),
                       type = attr(df, "type"), combined.weights = TRUE,
                       scale = attr(df, "scale"), rscales = attr(df, "rscales"),
                       mse = TRUE)
}
}
}
