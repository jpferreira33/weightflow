% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/variance.R
\name{jackknife_weights}
\alias{jackknife_weights}
\title{Recipe-aware delete-a-PSU jackknife replicate weights}
\usage{
jackknife_weights(
  object,
  strata = NULL,
  psu = NULL,
  lonely_psu = c("certainty", "collapse"),
  cores = 1L,
  progress = TRUE
)
}
\arguments{
\item{object}{a weighting_spec (inert recipe) or a prepped weighting_spec.
Pass the recipe \emph{before} \code{prep()}: the jackknife preps it once per replicate.}

\item{strata}{name of the stratum column, or NULL for a single stratum.}

\item{psu}{name of the PSU column, or NULL to delete one unit at a time.}

\item{lonely_psu}{how to treat strata with a single PSU: "certainty"
(default) skips them (no variance) and warns; "collapse" merges them into a
pseudo-stratum so they yield delete-a-PSU replicates.}

\item{cores}{number of parallel workers for the replicates (default 1 =
serial). With \code{cores > 1} the replicate re-preps run in parallel via
\code{parallel::mclapply} (forking; serial on Windows). For a deterministic
recipe the result is identical to the serial run.}

\item{progress}{print progress every 25 replicates (serial only).}
}
\value{
An object of class \code{weightflow_jack} with the \code{replicates} matrix
(units x replicates), the point \code{weights}, the per-replicate stratum and
stratum size (used by \code{jackknife_estimate()}), and the design metadata.
}
\description{
Builds jackknife replicate weights by deleting one primary sampling unit
(PSU) at a time and re-running the \strong{entire} weighting recipe on each
replicate. This is the deterministic sibling of \code{\link[=bootstrap_weights]{bootstrap_weights()}}: same
recipe-aware variance, no random number generation, and a replicate count
fixed by the design rather than chosen by the analyst.
}
\details{
For a stratum \eqn{h} with \eqn{n_h} PSUs, the replicate that deletes PSU
\eqn{i} zeros the base weight of that PSU and inflates the remaining PSUs of
the stratum by \eqn{n_h/(n_h-1)}; other strata are unchanged. There is one
replicate per PSU. Strata with a single PSU contribute no variance and are
skipped. This is the stratified jackknife (JKn); with \code{strata = NULL} it is
the unstratified jackknife (JK1), and with \code{psu = NULL} each unit is its own
PSU (delete-one-unit jackknife).
}
\examples{
spec <- weighting_spec(sample_one, base_weights = pw) |>
  step_calibrate(method = "raking",
                 margins = list(region = c(table(population$region))))
jk <- jackknife_weights(spec, strata = "region", psu = "psu", progress = FALSE)
jack_total(jk, "employed")
}
\seealso{
Other variance estimation:
\code{\link[=as_svydesign]{as_svydesign()}},
\code{\link[=bootstrap_estimate]{bootstrap_estimate()}},
\code{\link[=bootstrap_weights]{bootstrap_weights()}},
\code{\link[=collect_replicate_weights]{collect_replicate_weights()}},
\code{\link[=jackknife_estimate]{jackknife_estimate()}}
}
\concept{variance estimation}
