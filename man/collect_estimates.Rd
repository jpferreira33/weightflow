% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimation-spec.R
\name{collect_estimates}
\alias{collect_estimates}
\title{Evaluate an estimation pipeline}
\usage{
collect_estimates(est)
}
\arguments{
\item{est}{a \code{weightflow_estimation} from \code{\link[=step_estimate]{step_estimate()}}.}
}
\value{
a \code{weightflow_estimation_result}: one row per (estimand x domain cell) with
\code{estimate}, \code{se}, \code{ci_lower}, \code{ci_upper} and (for changes) \code{rho}. The object also
carries a row-aligned \code{detail} frame (confidence level, effective replicates, the
two wave levels behind a change and the overlap design effect) and the pipeline's
\code{filters}, so \code{\link[=report_panel]{report_panel()}} can document the estimates without re-running them.
}
\description{
Runs every estimand of a \code{weightflow_estimation} across the cross of its \code{\link[=step_domain]{step_domain()}}
columns, using the coordinated replicates for the variance, and returns a tidy table.
}
\seealso{
\code{\link[=step_estimate]{step_estimate()}}, \code{\link[=report_panel]{report_panel()}}
}
