% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transitions.R
\name{boot_transition}
\alias{boot_transition}
\title{Transition matrix with per-cell bootstrap standard errors}
\usage{
boot_transition(
  boot,
  from,
  to,
  states = NULL,
  format = c("row", "col", "joint", "counts")
)
}
\arguments{
\item{boot}{a \code{weightflow_boot} from \code{\link[=bootstrap_weights]{bootstrap_weights()}} on the longitudinal recipe.}

\item{from, to, states, format}{as in \code{\link[=transition_matrix]{transition_matrix()}}.}
}
\value{
a \code{weightflow_transition_boot} with \code{estimate}, \code{se}, \code{ci_lower}, \code{ci_upper} matrices.
}
\description{
Like \code{\link[=transition_matrix]{transition_matrix()}}, but every cell also gets a standard error and confidence interval
from the bootstrap replicate weights of a longitudinal-weight \code{\link[=bootstrap_weights]{bootstrap_weights()}} object, by
re-tabulating the flow within each replicate. The recipe is re-run per replicate, so the
uncertainty of the longitudinal weighting propagates into the flow SEs.
}
\seealso{
\code{\link[=transition_matrix]{transition_matrix()}}, \code{\link[=bootstrap_weights]{bootstrap_weights()}}
}
