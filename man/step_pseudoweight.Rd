% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adjust-pseudoweight.R
\name{step_pseudoweight}
\alias{step_pseudoweight}
\title{Pseudo-weights for a non-probability sample against a reference}
\usage{
step_pseudoweight(
  spec,
  reference,
  formula,
  engine = c("logit", "tree", "forest", "boost"),
  num_classes = NULL,
  crossfit = NULL,
  crossfit_seed = NULL,
  id = NULL
)
}
\arguments{
\item{spec}{a non-probability \code{weighting_spec}.}

\item{reference}{a \code{\link[=reference_sample]{reference_sample()}} (the probability reference with its
design weights). Pass the reference's replicate weights through
\code{reference_sample(replicates = )} to propagate its sampling variance through
the recipe-aware bootstrap: each replicate refits the propensity from the
paired reference replicate. Without them the reference is treated as fixed,
so the bootstrap reflects only the variability of the non-probability sample
(which it resamples as a with-replacement sample of units, a slightly
conservative approximation when that sample is a large fraction of the
population).}

\item{formula}{one-sided formula of the covariates shared by both samples,
e.g. \code{~ sex + age + region}.}

\item{engine}{propensity learner: \code{"logit"} (default), \code{"tree"}, \code{"forest"} or
\code{"boost"}.}

\item{num_classes}{NULL (default, direct \code{1/pi}) or an integer: group the
fitted propensities into that many quantile classes and use the class-average
pseudo-weight, which is more robust to a misspecified model.}

\item{crossfit, crossfit_seed}{optional K-fold cross-fitting of the propensity
(recommended for the flexible learners), and its seed.}

\item{id}{optional stable step id.}
}
\value{
the input \code{weighting_spec} with this step appended.
}
\description{
For a non-probability sample (opt-in panel, volunteer or river sample) with no
design weights, \code{step_pseudoweight()} estimates each unit's \emph{participation
propensity} \code{p} against a probability \code{reference_sample()} and assigns the
pseudo-weight \code{(1 - p)/p} (the participation odds; Elliott and Valliant 2017),
which inflates each unit to the population so the weights sum to the reference's
estimated population size. It stacks the non-probability sample and the
reference internally (the participation indicator and the two samples' weights
are built for you), fits the propensity, and returns the pseudo-weight on the
non-probability units only; the reference is used to train the model and then
dropped.
}
\details{
The recipe must be a non-probability spec: \code{weighting_spec(..., nonprob = TRUE)}. This step is the inverse-propensity (IPW) route; you can instead, or
additionally, calibrate to a \code{reference_sample()} with \code{\link[=step_calibrate]{step_calibrate()}} /
\code{\link[=step_model_calibration]{step_model_calibration()}} (mass imputation / model-based), and combining both
gives the doubly robust estimator.
}
\references{
Elliott, M. R. and Valliant, R. (2017). Inference for non-probability samples.
Statistical Science 32(2), 249-264.
}
\seealso{
\code{\link[=reference_sample]{reference_sample()}}, \code{\link[=step_calibrate]{step_calibrate()}}, \code{\link[=step_model_calibration]{step_model_calibration()}}

Other weighting steps:
\code{\link[=step_assert]{step_assert()}},
\code{\link[=step_calibrate]{step_calibrate()}},
\code{\link[=step_drop_ineligible]{step_drop_ineligible()}},
\code{\link[=step_model_calibration]{step_model_calibration()}},
\code{\link[=step_nonresponse]{step_nonresponse()}},
\code{\link[=step_nr_sensitivity]{step_nr_sensitivity()}},
\code{\link[=step_rescale]{step_rescale()}},
\code{\link[=step_round]{step_round()}},
\code{\link[=step_select_within]{step_select_within()}},
\code{\link[=step_trim]{step_trim()}},
\code{\link[=step_trim_calibrated]{step_trim_calibrated()}},
\code{\link[=step_trim_weights]{step_trim_weights()}},
\code{\link[=step_unknown_eligibility]{step_unknown_eligibility()}}
}
\concept{weighting steps}
