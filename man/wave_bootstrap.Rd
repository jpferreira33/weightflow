% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/variance-panel.R
\name{wave_bootstrap}
\alias{wave_bootstrap}
\title{Coordinated bootstrap across panel waves}
\usage{
wave_bootstrap(
  specs,
  replicates = 500L,
  strata = NULL,
  psu = NULL,
  m = NULL,
  seed = NULL,
  refit_steps = "all",
  resample = c("multinom", "binom"),
  progress = TRUE
)
}
\arguments{
\item{specs}{a \strong{named} list of \code{weighting_spec} objects, one per wave; the names are
the wave labels used by \code{\link[=change_estimate]{change_estimate()}}.}

\item{replicates}{number of bootstrap replicates.}

\item{strata, psu}{column names of the design strata and PSU, present in every wave's
data. The PSU id must be \strong{consistent across waves} (the same value = the same PSU):
that consistency is what makes the coordination possible. \code{strata = NULL} treats the
whole sample as one stratum; \code{psu = NULL} treats each row as its own PSU.}

\item{m}{optional PSU resample size per stratum (default \code{n_h - 1}, the Rao-Wu choice).}

\item{seed}{optional integer seed for the permanent uniforms (reproducibility).}

\item{refit_steps}{which recipe steps to re-run per replicate. \code{"all"} (default, the
honest recipe-aware bootstrap) re-preps the whole recipe, propagating the variance of
every step. \code{"calibration"} freezes everything up to the first calibration step and
re-runs only calibration per replicate -- the Statistics Canada LFS convention (the
Rao-Wu factor is applied to the frozen pre-calibration subweight). A character vector
of step classes (e.g. \code{c("step_nonresponse", "step_calibrate")}) splits at the first
one matched: the prefix is frozen, the suffix re-prepped. The point estimate always
uses the full recipe; only the replicate variance is affected.}

\item{resample}{the Rao-Wu resample scheme. \code{"multinom"} (default) draws an exact
multinomial per stratum (counts sum to \code{m_h}), the centred Rao-Wu draw, which gives an
unbiased replicate variance for composite/calibration estimators and for totals;
\code{"binom"} (legacy) draws an independent binomial per PSU (counts need not sum to
\code{m_h}), which loses the multinomial's negative between-PSU covariance and estimates an
uncentred variance -- only mildly high for a ratio or mean (~+8-10\%) but a large
overestimate for a \strong{total} (up to an order of magnitude), so use \code{"multinom"} when
estimating totals. Both
share the per-PSU uniforms; the multinomial coordinates across rotating waves exactly
when the PSU sets match (identical or disjoint waves) and approximately otherwise.}

\item{progress}{show a progress message per wave.}
}
\value{
an object of class \code{weightflow_wave_boot}: per-wave point weights, replicate
matrices (aligned by replicate index across waves), the per-wave data, and the design.
}
\description{
Builds recipe-aware bootstrap replicate weights for two or more waves that are
\strong{coordinated} by PSU: a PSU present in several waves gets the same resampling in
all of them, so the sampling covariance between waves -- which the overlap of a
rotating panel induces -- is captured. Feed the result to \code{\link[=change_estimate]{change_estimate()}} for the
honest variance of a net change. Ignoring the coordination (a separate per-wave
bootstrap) over-estimates the variance of change whenever the waves overlap and are
correlated.
}
\details{
Self-contained: it does not modify \code{\link[=bootstrap_weights]{bootstrap_weights()}}; each wave's recipe is
re-run through \code{\link[=prep]{prep()}} on perturbed base weights, exactly as the single-sample
bootstrap does, but with a PSU-coordinated Rao-Wu draw shared across waves.

Composite (CRE) recursion is handled automatically: when a wave's recipe holds a
\code{\link[=step_cre]{step_cre()}} whose \code{previous} is the preceding wave, that wave's estimated control
totals \verb{Zhat*} are re-estimated per replicate from the previous wave's replicate-b
weights (not the frozen point weights), so the reported variance includes the
sampling variance of \code{Zhat} itself. Waves are processed in order and each wave's
replicate matrix is carried into the next, giving the full coordinated variance of
the recursive estimator. (Assumes each \code{step_cre}'s \code{previous} is the immediately
preceding wave in \code{specs}; otherwise that step keeps its fixed point \code{previous}.)
}
\examples{
t1 <- subset(panel_ine, ola == 1 & disp == "R")
t2 <- subset(panel_ine, ola == 2 & disp == "R")
wb <- wave_bootstrap(
  list(T1 = weighting_spec(t1, base_weights = w_base),
       T2 = weighting_spec(t2, base_weights = w_base)),
  replicates = 200, strata = "estrato", psu = "psu", seed = 1, progress = FALSE)
change_estimate(wb, function(w, d) weighted.mean(d$desocupado, w, na.rm = TRUE))
}
\seealso{
\code{\link[=change_estimate]{change_estimate()}}, \code{\link[=bootstrap_weights]{bootstrap_weights()}}, \code{\link[=panel_design]{panel_design()}}
}
