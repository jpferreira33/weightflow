% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/variance-panel.R
\name{wave_jackknife}
\alias{wave_jackknife}
\title{Coordinated delete-one jackknife across panel waves}
\usage{
wave_jackknife(
  specs,
  strata = NULL,
  psu = NULL,
  refit_steps = "all",
  progress = TRUE
)
}
\arguments{
\item{specs}{a \strong{named} list of \code{weighting_spec} objects, one per wave; the names are
the wave labels used by \code{\link[=change_estimate]{change_estimate()}}.}

\item{strata, psu}{column names of the design strata and PSU, present in every wave's
data. The PSU id must be \strong{consistent across waves} (the same value = the same PSU):
that consistency is what makes the coordination possible. \code{strata = NULL} treats the
whole sample as one stratum; \code{psu = NULL} treats each row as its own PSU.}

\item{refit_steps}{which recipe steps to re-run per replicate; see \code{\link[=wave_bootstrap]{wave_bootstrap()}}.
\code{"all"} (default) re-preps the whole recipe; \code{"calibration"} freezes the prefix and
re-runs only calibration (StatCan LFS convention).}

\item{progress}{show a progress message per wave.}
}
\value{
an object of class \code{weightflow_wave_jack} accepted by \code{\link[=change_estimate]{change_estimate()}};
replicate columns are aligned across waves by the union of PSU ids (a PSU absent
from a wave, or alone in its stratum, yields that wave's point weights, i.e. a
zero jackknife contribution).
}
\description{
Deterministic counterpart of \code{\link[=wave_bootstrap]{wave_bootstrap()}}: builds delete-one-PSU replicate
weights that are \strong{coordinated} across waves (the same PSU is removed from every
wave simultaneously), so the sampling covariance the overlap induces is captured
without any randomness. Feed the result to \code{\link[=change_estimate]{change_estimate()}} for an honest,
reproducible variance of a net change. Meant as the cheap exact oracle to validate
\code{\link[=wave_bootstrap]{wave_bootstrap()}}; it is also a valid variance estimator in its own right.
}
\details{
Self-contained: each wave's recipe is re-run through \code{\link[=prep]{prep()}} on base weights with
the deleted PSU zeroed and its stratum mates reweighted by \eqn{n_h/(n_h-1)}; it
does not modify \code{\link[=bootstrap_weights]{bootstrap_weights()}} or touch \code{R/variance.R}.
}
\examples{
t1 <- subset(panel_ine, ola == 1 & disp == "R")
t2 <- subset(panel_ine, ola == 2 & disp == "R")
wj <- wave_jackknife(
  list(T1 = weighting_spec(t1, base_weights = w_base),
       T2 = weighting_spec(t2, base_weights = w_base)),
  strata = "estrato", psu = "psu", progress = FALSE)
change_mean(wj, "desocupado")
}
\seealso{
\code{\link[=wave_bootstrap]{wave_bootstrap()}}, \code{\link[=change_estimate]{change_estimate()}}
}
