% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sae.R
\name{as_sae_input}
\alias{as_sae_input}
\title{Direct estimates and design SEs per domain, ready for small-area estimation}
\usage{
as_sae_input(
  object,
  variable,
  by,
  type = c("mean", "total"),
  level = 0.95,
  cv_breaks = c(0.165, 0.33)
)
}
\arguments{
\item{object}{a \code{weightflow_boot} or \code{weightflow_jack} object (from
\code{\link[=bootstrap_weights]{bootstrap_weights()}} / \code{\link[=jackknife_weights]{jackknife_weights()}}).}

\item{variable}{name of the study variable.}

\item{by}{name(s) of the domain column(s); several are crossed.}

\item{type}{\code{"mean"} (default) or \code{"total"}.}

\item{level}{confidence level for the interval.}

\item{cv_breaks}{two increasing CV cut-points for the publishability rating
(default \code{c(0.165, 0.33)}, common in official statistics): a CV below the
first is \code{"publishable"}, between the two \code{"review"}, above the second
\code{"not publishable"}.}
}
\value{
A data frame with one row per domain: \code{domain}, \code{n} (active units),
\code{n_eff} (Kish effective sample size), \code{estimate}, \code{se}, \code{cv}, \code{ci_lower},
\code{ci_upper} and \code{rating}. Pass \code{estimate} and \code{se^2} (the sampling variance)
to a Fay-Herriot model.

Note for very small domains: the domain estimates share one bootstrap, and a
replicate in which any domain has no active unit is dropped for all domains
(a conservative choice). With many tiny domains this wastes replicates; use
more \code{replicates} in the bootstrap, or estimate sparse domains in a separate
call.
}
\description{
Small-area estimation (SAE) area-level models (Fay-Herriot) need, for each
domain, the \emph{direct} estimate, its \emph{design-based} variance and the effective
sample size. This function computes exactly those from a recipe-aware
replicate object, so the design-based ingredients flow into \code{emdi}, \code{sae} or
\code{hbsae} without leaving the weightflow variance machinery. It does not fit any
SAE model itself.
}
\details{
The domain standard error is the recipe-aware replicate SE (it re-runs the
whole recipe per replicate), so it already reflects nonresponse and
calibration, not just the final weights.
}
\examples{
spec <- weighting_spec(sample_survey, base_weights = pw) |>
  step_calibrate(method = "raking",
                 margins = list(region = c(table(population$region))))
boot <- bootstrap_weights(spec, replicates = 50, strata = "region",
                          psu = "psu", seed = 1)
as_sae_input(boot, "responded", by = "region")
}
\seealso{
\code{\link[=domain_summary]{domain_summary()}}, \code{\link[=bootstrap_estimate]{bootstrap_estimate()}}, \code{\link[=design_effect]{design_effect()}}

Other cascade audit:
\code{\link[=collect_propensities]{collect_propensities()}},
\code{\link[=collect_step_detail]{collect_step_detail()}},
\code{\link[=collect_weights]{collect_weights()}},
\code{\link[=domain_summary]{domain_summary()}},
\code{\link[=weight_factors]{weight_factors()}},
\code{\link[=weighting_alerts]{weighting_alerts()}}
}
\concept{cascade audit}
