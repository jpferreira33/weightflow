% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spec-steps-final.R
\name{step_assert}
\alias{step_assert}
\title{Assert quality conditions on the weights}
\usage{
step_assert(
  spec,
  max_deff = NULL,
  max_weight_ratio = NULL,
  min_n_eff = NULL,
  on_fail = c("error", "warning"),
  id = NULL
)
}
\arguments{
\item{spec}{a weighting_spec.}

\item{max_deff}{numeric or NULL. Maximum acceptable Kish design effect.}

\item{max_weight_ratio}{numeric or NULL. Maximum allowed final/base weight
ratio (per active unit).}

\item{min_n_eff}{numeric or NULL. Minimum acceptable effective sample size.}

\item{on_fail}{"error" (stop the cascade) or "warning".}

\item{id}{optional string: a stable identifier for this step, shown in the
recipe print-out; defaults to a derived \code{"<class>_<k>"}.}
}
\value{
The input \code{weighting_spec} with this checkpoint appended to its
recipe. The check is recorded only; it is evaluated when \code{prep()} is called
and does not modify the weights.
}
\description{
A checkpoint that leaves the weights untouched and instead verifies that they
meet quality thresholds at this point of the cascade, raising an error or a
warning when they do not. Use it to stop a production pipeline before bad
weights are published, in the spirit of a validation step inside a recipe.
}
\examples{
weighting_spec(sample_survey, base_weights = pw) |>
  step_assert(max_deff = 5, on_fail = "warning") |> prep()
}
\seealso{
Other weighting steps:
\code{\link[=step_calibrate]{step_calibrate()}},
\code{\link[=step_drop_ineligible]{step_drop_ineligible()}},
\code{\link[=step_model_calibration]{step_model_calibration()}},
\code{\link[=step_nonresponse]{step_nonresponse()}},
\code{\link[=step_nr_sensitivity]{step_nr_sensitivity()}},
\code{\link[=step_pseudoweight]{step_pseudoweight()}},
\code{\link[=step_rescale]{step_rescale()}},
\code{\link[=step_round]{step_round()}},
\code{\link[=step_select_within]{step_select_within()}},
\code{\link[=step_trim]{step_trim()}},
\code{\link[=step_trim_calibrated]{step_trim_calibrated()}},
\code{\link[=step_trim_weights]{step_trim_weights()}},
\code{\link[=step_unknown_eligibility]{step_unknown_eligibility()}}
}
\concept{weighting steps}
