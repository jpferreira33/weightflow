% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transitions.R
\name{boot_flows}
\alias{boot_flows}
\title{Gross-flow TOTALS with standard errors, plus net flows and margins}
\usage{
boot_flows(boot, from, to, states = NULL)
}
\arguments{
\item{boot}{a \code{weightflow_boot} from \code{\link[=bootstrap_weights]{bootstrap_weights()}} on the longitudinal recipe.}

\item{from, to, states}{as in \code{\link[=transition_matrix]{transition_matrix()}}.}
}
\value{
a \code{weightflow_flows} object with \code{counts}/\code{counts_se}, \code{net}/\code{net_se}, \code{origin}/\code{origin_se},
\code{dest}/\code{dest_se}, and \code{stayers}/\code{movers} (each with its SE).
}
\description{
The gross change is about the \strong{number of people} who move between states, i.e. totals. From a
longitudinal-weight \code{\link[=bootstrap_weights]{bootstrap_weights()}} object this returns, all with a bootstrap standard
error computed within each replicate: the from x to count matrix (the gross flows, in
population totals), the \strong{net} flow matrix \verb{i->j minus j->i}, and the margins -- the origin
totals (how many started in each state), the destination totals (how many ended in each), the
stayers (the diagonal) and the movers (off-diagonal).
}
\seealso{
\code{\link[=boot_transition]{boot_transition()}}, \code{\link[=transition_matrix]{transition_matrix()}}
}
