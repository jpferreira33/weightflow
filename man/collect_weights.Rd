% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prep.R
\name{collect_weights}
\alias{collect_weights}
\title{Extract the data with the computed weights}
\usage{
collect_weights(
  object,
  drop_zero = TRUE,
  keep_intermediate = FALSE,
  weight_name = ".weight"
)
}
\arguments{
\item{object}{a prepped object (output of prep()).}

\item{drop_zero}{logical. If TRUE, drops rows with final weight 0
(ineligible / nonresponse). Default TRUE.}

\item{keep_intermediate}{logical. If TRUE, adds one column per stage.}

\item{weight_name}{name of the final weight column. Default ".weight".}
}
\value{
data.frame.
}
\description{
Returns the sample as a \code{data.frame} with the final analysis weight attached
as a column, ready to hand to an estimation routine. By default the units that
left the cascade (weight 0) are dropped, so what comes back is the responding,
in-scope sample and its weights.
}
\examples{
fitted <- weighting_spec(sample_survey, base_weights = pw) |>
  step_nonresponse(respondent = responded, method = "weighting_class", by = "region") |>
  prep()
head(collect_weights(fitted))
}
