% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/variance.R
\name{bootstrap_weights}
\alias{bootstrap_weights}
\title{Bootstrap replicate weights that re-apply the recipe}
\usage{
bootstrap_weights(
  object,
  replicates = 200L,
  strata = NULL,
  psu = NULL,
  m = NULL,
  seed = NULL,
  progress = TRUE
)
}
\arguments{
\item{object}{a \code{weighting_spec} (or a prepped one) holding the recipe.}

\item{replicates}{number of bootstrap replicates.}

\item{strata, psu}{column names of the stratum and the PSU. If \code{psu} is NULL
each unit is its own PSU; if \code{strata} is NULL a single stratum is assumed.}

\item{m}{PSUs drawn per stratum (default \code{n - 1}).}

\item{seed}{optional RNG seed.}

\item{progress}{print progress every 25 replicates.}
}
\value{
An object of class \code{weightflow_boot} with the \code{replicates} matrix
(units x replicates), the point \code{weights}, and the design metadata.
}
\description{
Builds bootstrap replicate weights by resampling primary sampling units
(PSUs) with replacement within strata and re-running the whole recipe on
each replicate. Because every adjustment (nonresponse, calibration, ...) is
recomputed per replicate, the resulting replicate weights propagate the
variability introduced by each weighting stage.
}
\details{
The multiplier is the Rao-Wu rescaling bootstrap: within a stratum with
\eqn{n} PSUs, \eqn{m} PSUs are drawn with replacement (default
\eqn{m = n - 1}) and unit \eqn{i} in PSU \eqn{k} gets
\eqn{\lambda = 1 - \sqrt{m/(n-1)} + \sqrt{m/(n-1)}\,(n/m)\,t_k}, with
\eqn{t_k} the number of times its PSU was drawn.
}
\examples{
spec <- weighting_spec(sample_survey, base_weights = pw) |>
  step_calibrate(method = "raking",
                 margins = list(region = c(table(population$region))))
boot <- bootstrap_weights(spec, replicates = 50, strata = "region",
                          psu = "psu", seed = 1)
boot_total(boot, "responded")
}
