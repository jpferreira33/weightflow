% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/variance.R
\name{bootstrap_weights}
\alias{bootstrap_weights}
\title{Recipe-aware bootstrap replicate weights}
\usage{
bootstrap_weights(
  object,
  replicates = 200L,
  strata = NULL,
  psu = NULL,
  m = NULL,
  fpc = NULL,
  lonely_psu = c("certainty", "collapse"),
  seed = NULL,
  cores = 1L,
  progress = TRUE,
  .tp_component = c("full", "phase1", "phase2")
)
}
\arguments{
\item{object}{a \code{weighting_spec} (or a prepped one) holding the recipe.}

\item{replicates}{number of bootstrap replicates.}

\item{strata, psu}{column names of the stratum and the PSU. If \code{psu} is NULL
each unit is its own PSU; if \code{strata} is NULL a single stratum is assumed.}

\item{m}{PSUs drawn per stratum (default \code{n - 1}).}

\item{fpc}{optional first-stage finite-population correction: the name of a
column holding the first-stage sampling fraction f_h (constant within
stratum, in \verb{[0, 1]}), a single number applied to every stratum, or a numeric
vector named by stratum level. \code{NULL} (default) is the with-replacement
bootstrap (no correction). The correction folds \code{(1 - f_h)} into the Rao-Wu
rescaling (Rao, Wu and Yue 1992; Beaumont and Patak 2012); \code{f_h = 0}
reproduces the uncorrected result. Only available for the bootstrap.}

\item{lonely_psu}{how to treat strata with a single PSU (which a
with-replacement bootstrap cannot resample): "certainty" (default) treats
them as self-representing, so they contribute no bootstrap variance, and
warns; "collapse" merges the single-PSU strata into a pseudo-stratum (with
the smallest other stratum if there is only one), so they are resampled and
do contribute a (conservative) variance. For full control, build your own
collapsed stratum column and pass it as \code{strata}.}

\item{seed}{optional RNG seed.}

\item{cores}{number of parallel workers for the replicates (default 1 =
serial). With \code{cores > 1} the replicate re-preps run in parallel via
\code{parallel::mclapply} (forking; on Windows it falls back to serial). Results
are identical to the serial run: the resampling is drawn up front with the
seed and only the deterministic re-prep is parallelised.}

\item{progress}{print progress every 25 replicates (serial only).}

\item{.tp_component}{internal. For a two-phase recipe, \code{"full"} (default) draws
the coupled factor; \code{"phase1"} / \code{"phase2"} draw only the phase-1 or phase-2
component of the coupling. Used by \code{two_phase_variance()} to split
\eqn{V = V_1 + V_2}; not for direct use.}
}
\value{
An object of class \code{weightflow_boot} with the \code{replicates} matrix
(units x replicates), the point \code{weights}, and the design metadata.
}
\description{
Builds bootstrap replicate weights by resampling primary sampling units
(PSUs) with replacement within strata and re-running the \strong{entire} weighting
recipe on each replicate -- every estimated stage (nonresponse, calibration,
model calibration, trimming), not just one. Reach for this when those stages
are estimated from the sample and you want their uncertainty inside the
standard error, instead of conditioning on them as if they were known.
}
\details{
The multiplier is the Rao-Wu rescaling bootstrap: within a stratum with
\eqn{n} PSUs, \eqn{m} PSUs are drawn with replacement (default
\eqn{m = n - 1}) and unit \eqn{i} in PSU \eqn{k} gets
\eqn{\lambda = 1 - \sqrt{m/(n-1)} + \sqrt{m/(n-1)}\,(n/m)\,t_k}{lambda = 1 - sqrt(m/(n-1)) + sqrt(m/(n-1)) * (n/m) * t_k}, with
\eqn{t_k} the number of times its PSU was drawn.
}
\examples{
spec <- weighting_spec(sample_survey, base_weights = pw) |>
  step_calibrate(method = "raking",
                 margins = list(region = c(table(population$region))))
boot <- bootstrap_weights(spec, replicates = 50, strata = "region",
                          psu = "psu", seed = 1)
boot_total(boot, "responded")
# with a first-stage finite-population correction (per-stratum sampling fraction)
d <- sample_survey; d$f <- 0.1
spec_f <- weighting_spec(d, base_weights = pw)
bootstrap_weights(spec_f, replicates = 50, strata = "region", psu = "psu",
                  fpc = "f", seed = 1)
}
\seealso{
Other variance estimation:
\code{\link[=as_svydesign]{as_svydesign()}},
\code{\link[=bootstrap_estimate]{bootstrap_estimate()}},
\code{\link[=collect_replicate_weights]{collect_replicate_weights()}},
\code{\link[=jackknife_estimate]{jackknife_estimate()}},
\code{\link[=jackknife_weights]{jackknife_weights()}}
}
\concept{variance estimation}
