% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spec-steps-final.R
\name{step_trim_calibrated}
\alias{step_trim_calibrated}
\title{Trimmed calibration (range-restricted, totals-preserving)}
\usage{
step_trim_calibrated(
  spec,
  formula,
  lower = NULL,
  upper = NULL,
  calfun = c("linear", "raking"),
  by = NULL,
  cluster = NULL,
  equal_within_cluster = FALSE,
  maxit = 100L,
  tol = 1e-07
)
}
\arguments{
\item{spec}{a weighting_spec.}

\item{formula}{the auxiliaries whose calibration totals must be preserved
(right-hand side only), e.g. \code{~ region + age_group}. Usually the same
formula used in the preceding \code{step_calibrate()}.}

\item{lower, upper}{numeric. Absolute bounds on the trimmed weight. At least
one must be supplied; the other defaults to no bound. For positive variance,
use a positive \code{lower}. Each may be a single number (the same bound for every
unit) or, together with \code{by}, a named vector of bounds per subgroup (names =
the \code{by} group levels), for differentiated trimming.}

\item{calfun}{distance function: "linear" (default; the range-restricted
Euclidean distance) or "raking" (the multiplicative distance, which keeps
the adjustment factors positive).}

\item{by}{character or NULL. Subgroup column for differentiated bounds: with a
named-vector \code{lower}/\code{upper}, each subgroup is trimmed to its own bounds
while the preserved totals of \code{formula} stay global. NULL (default) uses the
same bounds for all units.}

\item{cluster}{character or NULL. Cluster (e.g. household) id column, for
integrative trimming (with \code{equal_within_cluster = TRUE}).}

\item{equal_within_cluster}{logical. If TRUE, integrative trimming: one
trimming factor per \code{cluster}, so weights stay constant within household.
The incoming weights must already be constant within cluster (e.g. from
\code{step_calibrate(equal_within_cluster = TRUE)}); the absolute bound then
applies to that common household weight. Requires \code{cluster}. FALSE
(default) trims each unit on its own.}

\item{maxit}{integer. Maximum iterations for the bounded solver.}

\item{tol}{numeric. Convergence tolerance for the bounded solver.}
}
\value{
The input \code{weighting_spec} with this step appended to its recipe. The
step is recorded only; it is evaluated when \code{prep()} is called.
}
\description{
Pulls already-calibrated weights into an absolute interval \verb{[lower, upper]}
without breaking the calibration: instead of capping and redistributing, it
re-solves a bounded calibration whose targets are the totals the incoming
weights already reproduce, optionally with its own band per subgroup through
\code{by}. It is the only one of the three trimming steps that leaves the
calibration totals intact.
}
\details{
The absolute-weight bound is imposed as a per-unit factor bound
\verb{w_new / w in [lower/w, upper/w]} on top of the incoming weights, using a
bounded (range-restricted) calibration with the truncated Deville-Sarndal
distances: the range-restricted Euclidean distance (\code{calfun = "linear"}, the
default) or the multiplicative one (\code{calfun = "raking"}). Weights inside the
range that are not needed to restore the totals stay put; the out-of-range ones
saturate at their bound and the rest move as little as possible. If the range
is too tight to preserve every total, the totals that cannot be met are relaxed
and a warning is raised.

This step is meant to run \strong{after} a \code{step_calibrate()}: it acts on the
positive incoming weights and leaves dropped units (weight 0) alone.
}
\examples{
# calibrate, then trim the calibrated weights into [5.5, 13.5] without breaking
# the region/sex totals (the calibrated weights of sample_survey live in ~[5.4, 14])
weighting_spec(sample_survey, base_weights = pw) |>
  step_calibrate(method = "raking",
                 margins = list(region = c(table(population$region)),
                                sex    = c(table(population$sex)))) |>
  step_trim_calibrated(~ region + sex, lower = 5.5, upper = 13.5) |>
  prep()
}
